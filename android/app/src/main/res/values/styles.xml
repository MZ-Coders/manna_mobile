<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Theme applied to the Android Window while the process is starting when the OS's Dark Mode setting is off -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <!-- Show a splash screen on the activity. Automatically removed when
             the Flutter engine draws its first frame -->
        <item name="windowSplashScreenBackground">@drawable/launch_background</item>
        <!-- Define o ícone animado da tela de abertura (seu logotipo) -->
<!--        <item name="android:windowSplashScreenBrandingImage" tools:targetApi="31">@drawable/manna_software</item>-->

        <item name="windowSplashScreenAnimatedIcon" tools:targetApi="31">@mipmap/ic_launcher_logo</item>

<!--        <item name="android:windowSplashscreenContent" tools:targetApi="31">@drawable/manna_software</item>-->

    </style>
    <!-- Theme applied to the Android Window as soon as the process has started.
         This theme determines the color of the Android Window while your
         Flutter UI initializes, as well as behind your Flutter UI while its
         running.

         This Theme is only used starting with V2 of Flutter's Android embedding. -->
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <!-- Define o ícone animado da tela de abertura (seu logotipo) -->
<!--        <item name="android:windowSplashScreenAnimatedIcon" tools:targetApi="31">@drawable/manna_software</item>-->

        <item name="windowSplashScreenBackground">?android:colorBackground</item>
        <item name="windowSplashScreenAnimatedIcon" tools:targetApi="31">@mipmap/ic_launcher_logo</item>


            </style>
        </resources>
